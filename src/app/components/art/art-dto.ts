export interface ArtDto{
            id: number;
            title: string;
            description: string;
            create_date: string;
            filename: string;
            visibility: string;
            thumbnail: String;
}